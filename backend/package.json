{
  "name": "stada-country-content-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Country-aware content API for STADA websites.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/db/migrate.js && node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/content-loader.js && node --check src/content-overrides.js && node --check src/db/client.js && node --check src/db/migrate.js && node --check src/products/repository.js",
    "db:migrate": "node src/db/migrate.js",
    "db:import-products": "node scripts/import-products-from-site.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
