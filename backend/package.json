{
  "name": "stada-country-content-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Country-aware content API for STADA websites.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/content-loader.js && node --check src/content-overrides.js"
  },
  "engines": {
    "node": ">=18"
  }
}
